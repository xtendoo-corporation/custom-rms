<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_sale_order_line_serial_selector_wizard_form" model="ir.ui.view">
        <field name="name">sale.order.line.serial.selector.wizard.form</field>
        <field name="model">sale.order.line.serial.selector.wizard</field>
        <field name="arch" type="xml">
            <form string="Vender como 2ª Mano / Ex-Demo">
                <field name="sale_order_line_id" invisible="1"/>
                <field name="lot_candidates" invisible="1"/>
                <group>
                    <field name="product_id"/>
                    <field name="state_code" widget="radio"/>
                </group>
                <field name="lot_ids" widget="serial_availability_picker"/>
                <p class="text-muted">
                    Las series ya asignadas en otro presupuesto/pedido activo aparecen
                    deshabilitadas, con el nombre del comercial que las tiene reservadas.
                </p>
                <footer>
                    <button name="action_confirm" type="object" string="Confirmar" class="btn-primary"/>
                    <button name="action_clear" type="object" string="Quitar todas las series"
                            confirm="¿Seguro que quieres quitar todos los números de serie de esta línea?"/>
                    <button string="Cancelar" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="view_order_form_inherit_rms_product_state" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.rms.product.state</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='order_line']/list/field[@name='name']" position="after">
                <field name="serial_state_name" invisible="1"/>
                <field name="serial_count" widget="serial_selector_button"
                       string="Estado" optional="show"
                       invisible="display_type"/>
            </xpath>
            <!-- El widget sol_o2m del campo order_line usa, en pantallas
                 estrechas (modo kanban), el <form> embebido aquí mismo —
                 no la lista de arriba ni sale.sale_order_line_view_form_readonly —
                 así que el botón de series hay que repetirlo también dentro
                 de este formulario anidado para que sea accesible en móvil. -->
            <xpath expr="//field[@name='order_line']/form//div[@name='ordered_qty']" position="after">
                <field name="serial_state_name" invisible="1"/>
                <label for="serial_count" string="Estado"/>
                <div name="serial_count">
                    <field name="serial_count" widget="serial_selector_button" nolabel="1"/>
                </div>
            </xpath>
        </field>
    </record>

    <!-- En pantallas estrechas, Odoo abre cada línea del pedido con
         sale.sale_order_line_view_form_readonly (no reutiliza el arch de
         la lista), así que el botón de series hay que añadirlo aquí
         también para que sea accesible desde móvil. -->
    <record id="view_sale_order_line_form_readonly_inherit_rms_product_state" model="ir.ui.view">
        <field name="name">sale.order.line.form.readonly.inherit.rms.product.state</field>
        <field name="model">sale.order.line</field>
        <field name="inherit_id" ref="sale.sale_order_line_view_form_readonly"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='product_uom_qty']" position="after">
                <field name="serial_state_name" invisible="1"/>
                <field name="serial_count" widget="serial_selector_button"
                       string="Estado" invisible="display_type"/>
            </xpath>
        </field>
    </record>
</odoo>
